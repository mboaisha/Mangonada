---

# "Common" packages
modules:
  - type: dnf
    install:
      packages:
        - fastfetch
# Move files to image
  - type: files
    files:
      - source: system/etc
        destination: /etc
      - source: system/usr
        destination: /usr
