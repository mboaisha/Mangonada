---

# Install and setup DankMaterialShell
modules:
  - type: dnf
    repos:
      copr:
        enable:
          - avengemedia/dms-git
    install:
      skip-unavailable: false
      packages:
        - dms
