- type: dnf
  repos:
    files:
      - https://github.com/terrapkg/subatomic-repos/raw/main/terra.repo
    cleanup: true
  install:
    skip-unavailable: false
    packages:
      - repo: terra
        packages:
          - mangowm
